// postcss.config.js (for Tailwind v4.0+)

export default {
    plugins: {
      // FIX: Use the dedicated PostCSS plugin package
      '@tailwindcss/postcss': {}, 
      //'autoprefixer': {},
    },
  };